// Firebase configuration (optional).
// Leave this file as-is until you create a Firebase project at https://console.firebase.google.com
// When you do, replace the placeholder values below with your project's config.
// The app will detect that values are filled in and unlock real account features.
// To keep using the app in local-only mode, do nothing here.

window.FIREBASE_CONFIG = {
  apiKey: '',
  authDomain: '',
  projectId: '',
  storageBucket: '',
  messagingSenderId: '',
  appId: '',
};
